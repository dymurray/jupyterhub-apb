{
    "kind": "ImageStream",
    "apiVersion": "v1",
    "metadata": {
        "name": "jupyterhub",
        "namespace": "{{ namespace }}",
        "labels": {
            "build": "jupyterhub"
        }
    },
    "spec": {
        "lookupPolicy": {
            "local": true
        }
    }
}
